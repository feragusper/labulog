# Project instructions

- Default git workflow: commit and push directly to `main` after changes, without asking for confirmation each time. No feature branches / PRs unless user asks explicitly otherwise.
